# 行情与 API 设置

适用：原生版 1.0.2 build 9。入口在「设置 → 行情数据 → 行情来源」，保存后可立即同步。

## 报价模式

| 模式 | 使用方式 |
| --- | --- |
| 自动 | 盘中使用所选接口；开盘前、收盘确认后及已知休市日使用已完成日线 |
| 最近收盘价 | 使用已完成日线，不使用 Finnhub Key 请求历史收盘 |
| 所选接口最新报价 | 使用选择的来源；选择 Yahoo 时仍是收盘数据，选择 Tiingo 时使用 IEX 参考报价 |

日历的「同步历史」与盘中接口独立。填写 Tiingo 日线 Key 后，Tiingo 为主来源、Yahoo 为备用来源；两者都缺少 Yahoo 已明确列出的某天时，再用 Nasdaq 的同日历史收盘补洞。页面显示实际美东报价日期；亚洲已经进入新一天时，最近美股收盘仍可能属于前一日。

## 数据源

Tiingo API Key 在 API 设置中填写，保存在 iOS Keychain，通过 `Authorization: Token …` 请求头发送。当前报价使用官方 IEX REST 批量端点 `GET /iex/{ticker[,ticker...]}`；历史日线使用 Tiingo EOD 端点。当前报价采用 Tiingo 参考价字段，原始 IEX last/bid/ask 等字段可能要求额外交易所权限。是否实时、哪些字段可用和请求额度均取决于账户，不作实时性保证。

多数美股日线通常在美东收盘后提供，后续修正会在再次同步时覆盖自动来源的旧值。Tiingo 不可用时历史数据自动退回 Yahoo；Yahoo 明确缺口再由 Nasdaq 补齐。不填写 Key 时，仍可使用 Yahoo 与 Nasdaq 收盘价。

Yahoo、Nasdaq 无需 Key。Finnhub 需填写自己的 Key，通过 `X-Finnhub-Token` 请求头发送到其报价接口。是否实时、是否限流取决于账户权限，App 不附带共享 Key 或行情订阅。

自定义接口需 HTTPS，地址包含 `{symbol}`。可选 Key 作为 Bearer 凭据发送给你填写的服务。接口需要返回应用约定的 JSON，任意券商 API 不一定能直接使用：

```json
{
  "symbol": "AAPL",
  "currency": "USD",
  "price": "230.50",
  "timestamp": "2026-09-16T14:30:00Z"
}
```

价格须有效且大于零，代码与请求相符，币种为 USD，时间携带时区。接口不匹配时需要自行提供适配服务。

## 刷新与保存

可选择仅手动、60 秒或 5 分钟刷新。Tiingo 持仓报价一次合并请求多个去重代码，缓存 TTL 为 60 秒；并发相同请求合并。收到 HTTP 429 后至少冷却 60 秒，不自动重试。自动任务仅在前台运行，刷新间隔不等于数据源更新速度。失败时保留旧价并显示错误；缓存旧价回退时明确标为较早，不写入零价。

原生版的报价及历史收盘会保存到原生账本。自动来源失败时保留已有数据，不写入零价。若所有来源仍缺数据，可点收益日历中的缺失日期，在详情里手工补录；手工收盘价优先于之后的自动同步，可再次录入修正。盘中价不是只存在于内存。

设置和 Key 保存在系统钥匙串，不包含在账本备份中。网络请求只向 Tiingo、Yahoo、Nasdaq、Finnhub 或自定义接口发送股票代码及该服务需要的凭据，不发送交易股数、成交金额、备注或银行 PDF。原生请求使用 URLSession，不依赖浏览器 CORS。应用源码采用 MIT 许可不代表 Tiingo 数据可再分发；应遵守账户条款并另行取得所需授权。
