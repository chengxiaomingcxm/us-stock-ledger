# 行情与 API 设置

适用：原生版 1.0.0。入口在「设置 → 行情数据 → 行情来源」，保存后可立即同步。

## 报价模式

| 模式 | 使用方式 |
| --- | --- |
| 自动 | 盘中使用所选接口；开盘前、收盘确认后及已知休市日使用 Yahoo 完成日线 |
| 最近收盘价 | 使用 Yahoo 已完成日线，不使用 Finnhub Key 请求历史收盘 |
| 所选接口最新报价 | 使用选择的来源；选择 Yahoo 时仍是收盘数据 |

日历的「同步历史」独立使用 Yahoo 日线，不会随最新报价接口切换。页面显示实际美东报价日期；亚洲已经进入新一天时，最近美股收盘仍可能属于前一日。

## 数据源

Yahoo 无需 Key。Finnhub 需填写自己的 Key，通过 `X-Finnhub-Token` 请求头发送到其报价接口。是否实时、是否限流取决于账户权限，App 不附带共享 Key 或行情订阅。

自定义接口需 HTTPS，地址包含 `{symbol}`。可选 Key 作为 Bearer 凭据发送给你填写的服务。接口需要返回应用约定的 JSON，任意券商 API 不一定能直接使用：

```json
{
  "symbol": "AAPL",
  "currency": "USD",
  "price": "230.50",
  "timestamp": "2026-09-16T14:30:00Z"
}
```

价格须有效且大于零，代码与请求相符，币种为 USD，时间携带时区。接口不匹配时需要自行提供适配服务。

## 刷新与保存

可选择仅手动、60 秒或 5 分钟刷新。自动任务仅在前台运行，刷新间隔不等于数据源更新速度。失败时保留旧价并显示错误，不写入零价。

原生版的报价及可用的上一收盘基准会保存到原生账本；同日手动报价优先。盘中价不是只存在于内存。

设置和 Key 保存在系统钥匙串，不包含在账本备份中。网络请求发送股票代码及所选服务需要的凭据，不发送交易股数、成交金额、备注或银行 PDF。原生请求使用 URLSession，不依赖浏览器 CORS。
