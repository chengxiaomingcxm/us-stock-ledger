# 持仓账本帮助文档

适用版本：1.22。应用内入口：设置 → 帮助文档，可离线查阅。

## 首页与持仓

大号持有收益是当前持仓的浮动收益，即持仓市值减持仓成本，不是今日涨跌。收益率以当前持仓成本为分母；成本为零或报价缺失时显示 —。

点底部操作栏正中间的“记一笔”添加买入或卖出。首页只保留简短行情状态，点该行查看具体失败原因。点眼睛可隐藏首页金额、数量及收益率；此开关仅作用于首页和持仓详情，本次运行中保留，交易和收益页仍显示金额。点股票卡片打开持仓详情，点报价日期可手动更新价格。可按代码、市值或收益排序，筛选全部、盈利、亏损和待报价。

“报价覆盖”指已有价格的持仓数量，不代表实时行情覆盖。每只股票显示真实来源及报价日期；当前估值可能混合多个时点。


## 移动平均成本

买入金额和手续费加入持仓成本；卖出按卖出前的平均成本扣减。已实现收益为卖出金额减手续费，再减去卖出部分的成本。


## 股价与时间

可在行情 API 设置选择最新报价，前台按所选间隔刷新；盘中价只用于当前估值，不写入收益日历或备份。打开 App 或返回前台时检查 Yahoo Finance 收盘历史，15 分钟内不重复自动请求。只使用已结束交易日的日线收盘价，更新后自动重算市值与浮动收益；周末和休市日保留最近交易日。收盘后预留 15 分钟等待数据。每只股票显示实际报价日期，日期不同的报价会混合估值，并非统一时点快照。


## 报价来源与手动修正

Yahoo Finance 公共行情接口可能延迟、限流或变更，无法保证持续可用。失败时保留原报价，收益可能尚未更新。手动报价在同日优先；后续交易日收盘价会继续自动更新。公司 Logo 来自 Financial Modeling Prep，缺失时显示代码首字母。


## 数据与升级

v1.22 继续读取第一版的本机账本和 JSON / JS 文件名备份（内容必须是 JSON），不改交易及成本算法。完整备份包含收益历史；通用备份保留交易、报价和备注。旧版可读取新版核心账本，但再次导出时不会保留收益历史，请另存完整备份。行情请求发送股票代码及你填写的接口密钥，Logo 请求发送股票代码；账本、数量、金额和备注不上传。不自动导入券商交易。


## 每日收益与恢复

交易日期请按美东成交日期录入。收益日历依据 SPY 已完成日线确认交易日；缺少前日或当日必要收盘价时显示待补全。发现拆股时受影响收益暂停计算，请核对账本。休市标记包含周末及已公布的 2026–2028 年 NYSE 假日。自动恢复记录仅保存在本机，最多 5 份、合计 8 MB，空间不足时淘汰最早记录；不能替代导出备份。


## 支持范围

支持美元美股和 ETF 的买入、卖出、收盘价同步与手动报价，最多 5,000 笔交易。暂不包含分红、拆股、做空、期权或税务成本计算。


## 行情 API 配置

在设置 → 行情 API 设置选择 Yahoo 收盘价、Finnhub 最新报价或自定义接口。Finnhub 需要你自己的 API Key；权限与报价延迟由供应商决定。

自定义接口使用 HTTPS GET，地址必须包含 {symbol}，会替换为股票代码；可选密钥以 Authorization: Bearer 请求头发送。返回如下 JSON：


```json
{"symbol":"AAPL","currency":"USD",
"price":"230.50",
"timestamp":"2026-09-16T14:30:00Z"}
```

时间必须带时区，价格大于零，代码与请求一致。此协议不自动适配所有第三方接口。网页预览需要服务端允许跨域。

可选每 60 秒、每 5 分钟或关闭定时刷新，另可手动更新。最新报价仅存于当前会话，重启后重新获取；最新价格不写入历史日收益或备份。超过 15 分钟标为较早报价，休市时属正常情况。失败保留已取得的价格。

密钥仅保存在本机 Preferences，不是加密保险库，不进入账本备份。换设备需重新配置。切换供应商时清空密钥输入，避免误用。接口请求发送股票代码和密钥，不发送交易数量、金额或备注。


## 备份、恢复与升级

定期导出完整备份并保存到应用外。备份文件未加密；卸载会删除本机账本与恢复记录。升级前先导出备份，使用原签名账号与应用身份覆盖安装，不先卸载旧版。

完整备份含交易、报价、备注与历史行情；通用备份不含历史，供旧版使用。导入前会预览并要求确认替换；重要修改前会保留恢复记录，最多 5 份。恢复记录不能代替外部备份。


## 收益日历与资金记录

每日收益 = 期末市值 − 期初市值 + 卖出净收入 − 买入含费支出。新增本金不会直接算作盈利。点日期查看准确金额及个股贡献，k 表示千美元。

曲线使用各日收盘累计收益，可能不同于当前最新价或手动价格下的累计收益。缺失行情显示待补全或未确认，不当作零收益；本月合计只包含完整交易日。

资金记录中的买入支出含费用，卖出收入已扣费用；它们是交易现金流，不是账户现金余额。买卖按日期计算，同一天按录入顺序，编辑保留原顺序。

