# 数据兼容说明

## 原生版 1.0 与此前原生 2.0

本次是产品版本重新编号，不是账本降级或迁移。

- 应用版本：1.0.0；构建号：5。
- 应用标识仍为 `com.personal.stockledger`。
- 账本仍保存到本机 Documents 下的 `ledger-v2.json`。
- 交易、报价、现金记录、期初现金和历史收盘价的 Codable 结构不变。
- API 设置继续使用现有系统钥匙串项目，外观等偏好继续使用原来的键。

因此从已验收的 2.0.0 build 4 覆盖安装时，应用继续读取同一份原生账本；原生 2.0 导出的备份仍按原有恢复逻辑读取。不要因为产品版本叫 1.0，就重命名账本文件或重置存储键。

## 旧 Web 版

旧 Web 版 1.26 及更早版本使用不同的数据结构和 Capacitor 存储。原生版不自动读取或迁移这些数据，也不承诺直接导入其完整备份。

旧 Web 版升级前应单独保留旧备份；需要转入原生版时，使用受支持的交易和资金导入方式，并核对交易顺序、持仓成本和现金。旧标签、Release 和历史文档保留，不用新原生版的“1.0”解释旧版兼容表。

## 备份边界

备份包含原生账本数据，不包含 API Key 或外观设置。恢复是替换，不是合并；导入确认前先另存现有备份。原生版没有旧 Web 版文档所述的双槽恢复记录功能，不能依赖那套机制找回数据。

本地保存采用原子写入，保存成功后更新内存状态；这不能替代独立备份。卸载、签名身份变化或手动清除数据可能使本机账本不可用。

维护时应使用虚构数据验证旧原生备份读取，不把真实账本、银行 PDF、CSV 或密钥提交到仓库。
