# 数据兼容约定

## 兼容承诺与边界

| 数据 | v1.0 | v1.1 | v1.2 |
| --- | --- | --- | --- |
| 交易、碎股、日期、费用、备注、当前报价 | 可读取、计算、导出 | 可读取、计算、导出 | 可读取、计算、导出 |
| 自动报价来源与检查时间 | 忽略可选字段 | 支持 | 支持 |
| 每日收益所需的历史收盘价、交易日、拆股标记 | 忽略，重新导出时丢弃 | 忽略，重新导出时丢弃 | 完整备份保留 |
| 本机恢复记录 | 无入口 | 无入口 | 本机单独保存，不包含在导出文件中 |

“各版本通用”指核心交易账本与报价可往返，不能让旧程序拥有新增功能。需要回旧版时，请同时另存一份 v1.2 完整备份；从旧版重新导出的文件中无法恢复被旧程序舍弃的历史行情。

## 文件与存储

- 核心文件版本继续为 `version: 1`，币种 USD，方法 moving-average。应用版本 1.2.0 与文件版本是两个概念。
- `trades` 和 `quotes` 的必需字段及原计算规则不变。新根字段 `history` 可选，结构版本为 1，包括 `closes`、`sessions`、`splits` 和可选 `checkedAt`。
- 完整备份包含 history；通用备份省略 history。两者都保留所有交易和报价。
- 第一版 `.json` / `.js` 文件名均可导入，但内容必须是 JSON，不执行 JavaScript。
- 原生应用标识仍是 `com.personal.stockledger`；原双槽键 `stock-ledger-v1-0` / `stock-ledger-v1-1` 与 revision 格式不变。不重建或清空旧账本。
- 新恢复记录放在独立的 `stock-ledger-recovery-v1`，最多 5 份、合计 8 MB。编辑、删除、手动修改报价、导入、恢复之前先写入恢复记录；这一步失败则不覆盖账本。自动行情同步和新建交易不挤占这 5 份记录。
- 备份仍限制 8 MB；损坏或不支持的数据明确报错，不静默丢弃。历史收盘价最多 25,000 条，超过容量按完整的最早日期淘汰，交易日最多 4,000 个。
- 恢复记录仅在本机，卸载后同时消失。应用覆盖安装仍取决于签名工具保持原账号与应用身份，安装前必须另存备份。

## 1.26 现金账本与 CSV 导入

- 账本新增可选根字段 `cash`：`opening`（期初余额，最多一份）和 `records`（入金 / 出金 / 分红 / 费用，最多 2,000 笔）。旧版账本和备份没有该字段，读取后自动按未初始化处理，不改变交易、报价与历史数据。
- 旧版本（1.25 及更早）打开包含 cash 的备份时会忽略该字段；旧版重新导出后不会保留现金记录。两台 1.26 设备之间请用完整备份，通用备份不含现金记录和历史行情。
- 现金记录不影响持仓成本与已实现收益；入金、出金不计入盈亏，期初日之后的买卖联动现金（买入扣含费支出、卖出加扣费收入）。期初余额按“期初日当天开始前”定义，之前的交易与资金流水只备查、不重复计入。
- 交易与现金记录新增可选 `externalId` 字段（券商成交编号 / 流水编号），仅用于导入去重；同账本内不得重复。旧版忽略该字段。CSV 导入不写入任何密钥或登录凭据；导入前必须先预览并确认，导入、编辑、删除现金记录前先写本机恢复记录，校验失败不覆盖账本。核心存储键、`version: 1`、应用标识和双槽 revision 格式保持不变。

## 持续维护要求

以后新增数据使用可选字段并更新兼容表；若不得不改变核心格式，提供明确迁移和通用导出，不能只修改文件版本号。每次发布运行 `tests/history.test.ts` 中的真实旧版解析器测试，保留原始虚构备份样本；不得用真实用户账本作为公开测试数据。

测试中冻结的旧版代码来源：v1.0 提交 `e06032994ca92d78bd343275accb1f66cb92cb80`，v1.1 标签 `v1.1.0`。测试验证新版完整备份、通用备份、旧版重导出的核心账本，以及本机升级与恢复失败保护。

## 1.21 行情设置与临时报价

核心账本及旧存储键不变，应用标识保持 com.personal.stockledger。API 配置使用独立的 stock-ledger-market-settings-v1 Preferences 键，包含用户自行填写的密钥，不纳入任何账本备份。盘中报价仅在当前运行会话用于估值，退出进程后重新请求；导出备份保留已保存的收盘/手动报价。因此恢复备份不会恢复 API 设置或盘中快照。

## 1.22 界面更新

新增隐藏金额、列表筛选和排序仅作用于显示，状态保存在当前会话内存，不写入交易账本或备份。首页的持有收益仍是浮动收益，不作为今日收益使用。核心存储键、version: 1、应用标识和 API 配置键均保持原样。帮助文档随应用打包，可离线使用。
