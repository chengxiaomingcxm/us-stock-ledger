# 数据兼容说明

## 1.0 是数据基线

产品基线是**原生 SwiftUI 版 1.0**（应用标识 `com.personal.stockledger`）。

- 账本保存到本机 Documents 下的 `ledger-v2.json`。
- 交易、报价、现金记录、期初现金和历史收盘价的核心 Codable 字段自 1.0 起固定；新增元数据只能是可选字段，旧文件缺失时必须有安全默认值。
- API 设置使用系统钥匙串项目，外观等偏好使用固定的存储键；不要重命名账本文件或重置存储键。

**1.0 之前的所有测试版/预览版（含原生 2.0 测试版与旧 Web 版）不再是兼容目标**：
它们的账本与备份不保证能被读取、还原或以原样展示，也不要为它们继续保留兼容分支。

稳定承诺只对 **1.0 起写入的账本与备份** 有效：只要不抬高 `currentFormat`，后续版本继续可读。

build 9 给历史收盘价增加了可选的 `source` 元数据，用来保护用户手工核对的收盘价不被自动接口覆盖。旧账本没有该字段时按 `nil` 读取，价格、日期和计算口径不变；因此仍是 format 2，不需要迁移。较旧 App 虽可忽略未知字段读取账本，但降级后再次保存可能丢掉这项来源标记，不建议降级使用。

## note 的归属与格式代际

- **`Trade.note` / `CashRecord.note` 只装用户或银行/券商给的原文**，属于 source data：不翻译、不改写、不迁移。
- 系统自己的说明（如「汇丰月结单；交收日 X」「PAID BENEFITS 净额，税前金额与预扣税未披露」）**不再写进 `note`**，
  改由结构化字段在展示时生成：交易用 `source` + `settlementDate`，净额分红用 `source` + `tax`。
  这些说明因此天然跟随界面语言，也不会把数值拼进翻译键。
- 展示层只在 `note` **为空**时生成说明；`note` 里一旦有内容（用户写的、或 1.0 之前版本写进去的系统文案）就原样显示。
  系统文案永远不会覆盖已持久化的值——这是数据安全底线，与兼容目标无关。
- **格式代际：`format` 仍为 2**（`Ledger.currentFormat`）。本次只改了「写什么」与「怎么展示」：
  没有增删字段、没有改字段类型、也没有改到让旧文件被误读，所以新旧文件仍互相兼容，
  **不构成 breaking schema change**，也就不需要（更不应该）抬高版本号去掩盖一个其实兼容的结构。
- 载入与「从备份恢复」共用同一个解码入口（`LedgerStore.decode`）：**声明了更高 `format` 的文件会被明确拒绝**
  （进入写保护、提示更新 App，而不是提示「从备份恢复」——后者会用旧备份盖掉更完整的账本）；
  缺省 `format` 或 `format <= 2` 的文件照常读取。这样下一次真的需要换代时，边界已经是现成的。
- 维持边界的规则：**只有结构真的变了才抬高 `Ledger.currentFormat`**，并同步 `Ledger` 的字段、
  `tests/native/SafetyTests` 的格式用例与本文档。

## 旧 Web 版

旧 Web 版使用不同的数据结构和 Capacitor 存储。原生版不自动读取或迁移这些数据，也不承诺直接导入其完整备份。

旧 Web 版升级前应单独保留旧备份；需要转入原生版时，使用受支持的交易和资金导入方式，并核对交易顺序、持仓成本和现金。旧标签、Release 和历史文档保留，不用新原生版的“1.0”解释旧版兼容表。

## 备份边界

备份包含原生账本数据，不包含 API Key 或外观设置。恢复是替换，不是合并；导入确认前先另存现有备份。原生版没有旧 Web 版文档所述的双槽恢复记录功能，不能依赖那套机制找回数据。

本地保存采用原子写入，保存成功后更新内存状态；这不能替代独立备份。卸载、签名身份变化或手动清除数据可能使本机账本不可用。

维护时应使用虚构数据验证备份读取，不把真实账本、银行 PDF、CSV 或密钥提交到仓库。
