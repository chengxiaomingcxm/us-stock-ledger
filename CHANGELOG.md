# 更新记录

## 1.26 / 1.26.0 / build 9（2026-09-17，开发中）

- 收益页新增「现金账本」：设置期初余额后，记录入金、出金、手动分红（含预扣税费）和账户费用，余额自动汇总；入金出金不计入盈亏，分红按到账金额核对。
- 新增券商 CSV 导入：支持带表头的交易 / 资金流水 CSV（逗号、分号或制表符分隔），自动识别中英文列名，导入前展示字段映射、预览、错误行和疑似重复记录，确认后才写入；重复导入不会重复记账。
- 现金记录与 CSV 导入的交易均标记来源，可追溯；现金余额独立于持仓收益计算，总资产 = 现金 + 持仓市值。
- 旧账本自动兼容：现金记录为可选字段，旧备份可直接读取；期初余额需对照券商账单手动设置，无法从旧交易推断。完整备份包含现金记录，通用备份不含。
- 新增 31 项单元测试与 4 项浏览器流程，覆盖期初余额、入金出金、分红税费、CSV 映射 / 错误行 / 疑似重复与备份往返。

审查后修复（2026-09-17）：

- 买卖联动现金：期初日之后的买入支出（含费）扣减、卖出收入（扣费）增加，总资产 = 现金 + 市值；交易编辑、删除、撤销与导入后自动重算，不重复扣手续费。
- 期初余额按“期初日当天开始前”定义：之前的水流与交易视为已含在期初余额、只备查不重复计入；同日按日期归总。期初允许为零，不支持负数。
- 资金 CSV 增加币种列校验，非美元整行拒绝；数字严格校验千位分组、精度与格式，日期后仅允许合法时间。
- 取消类型映射后必须选择统一类型才能导入；确认按钮数量随勾选实时更新，零勾选时禁用。
- 去重区分“已导入同一记录”（券商编号 / 流水编号，提交层拒绝重复）与“数值相同独立记录”（疑似重复默认不勾选）；同日买卖按成交时间排序，无法确定顺序时要求勾选确认。
- 预览阶段按账本规则整体校验（超卖、超容量等提前提示）；提交阶段错误统一展示且账本不变。支持 UTF-8 / BOM / UTF-16 / GBK 解码，无法识别时拒绝写入；CSV 行号与带换行备注处理修正。
- 编辑导入交易保留 CSV 来源与编号；通用备份导出前提示将剔除的数据；恢复旧备份时提示将移除的现金记录。收益页新增“账户总收益”（证券收益 + 分红净额 − 费用），今日盈亏与收益日历仍只统计证券。
- iOS 工程版本统一为 1.26.0 / build 9，并新增版本一致性检查。

升级前先导出完整备份，使用原签名账号和应用标识覆盖安装，不要卸载旧版。详见 [1.26 升级说明](releases/v1.26.0.md)。

## 1.25 / 1.25.0 / build 8（2026-09-17）

- 修复旧日历、缺失个股收盘价、拆股、休市盘后报价和跨时区录入导致的今日盈亏误算。
- 新交易与手动报价默认美东日期，报价时间统一美东时区；异常旧记录提示核对，不自动改写。
- 修复放弃修改弹窗的焦点隔离，持仓详情记一笔自动带入股票代码。

- 首页新增「今日盈亏」：结合上一交易日收盘、当前价格、当天买卖与手续费计算，与「持有收益」分开展示并标注含义；缺少必要行情时显示待补全，不以零代替。休市日按最近收盘比较。
- 行情状态完善：每只持仓显示实际报价时间、来源与类型（最新报价 / 美股收盘 / 手动报价），休市判断与报价过期提示分开；刷新失败保留已有价格并提示原因。
- 交易页新增日期区间筛选，可与股票关键字、买卖类型组合；汇总所选范围内的笔数、股数、手续费与已实现收益。
- 记账表单自动聚焦股票代码，保存按钮滚动时保持可见；退出未保存表单时提醒确认。
- 首页大卡片缩减高度，金额等宽数字显示；持仓详情集中展示股数、成本、今日盈亏、报价状态与最近相关交易。
- 新增今日盈亏计算，保留原有交易成本计算、行情获取、账本存储位置及备份格式；最新价格仍只用于当前估值与今日盈亏，不写入收益日历或备份。

升级前先导出完整备份，使用原签名账号和应用标识覆盖安装，不要卸载旧版。详见 [1.25 升级说明](releases/v1.25.0.md)。

## 1.24 / 1.24.0 / build 7

- 行情来源入口从设置页移到首页 / 收益页的「行情状态」面板，点开后再点「更换行情来源」即可切换 Yahoo / Finnhub / 自定义 API。
- 设置页不再单独放行情 API 入口，界面更简洁。
- 修复收益页月份选择器在 320px 窄屏下的横向溢出。
- 65 项单元测试及 13 项浏览器流程通过；仍需 iPhone 真机覆盖安装确认。

升级前先导出完整备份，使用原签名账号和应用标识覆盖安装，不要卸载旧版。行情入口、数据时效、收益口径和密钥相关注意事项见 [1.24 升级说明](releases/v1.24.0.md)。

## 1.23 / 1.23.0 / build 6

- 设置新增「显示与提醒」：外观跟随系统 / 浅色 / 深色，涨跌颜色绿涨红跌 / 红涨绿跌，备份提醒每 7 天 / 每 30 天 / 关闭；选择保存在本机，重启后保留。
- 行情 API 密钥在 iOS 上改存系统钥匙串，不进账本备份；旧值首次使用时自动迁移，写入先校验再清理旧位置。
- 交易表单新增「一半 / 全部」快捷卖出、最近股票代码下拉、手续费默认沿用上一笔；保存后可撤销刚新增的交易。
- 首页在需要备份时显示「该备份账本了」提醒。
- 65 项单元测试及 13 项浏览器流程通过；仍需 iPhone 真机覆盖安装确认。

## 1.22 / 1.22.0 / build 5

- “记一笔”移到底部操作栏正中间；移除页面顶部和持仓标题旁的重复入口。

- 参考用户提供的卡片布局重做首页：大号持有收益、持仓市值、收益概览、紧凑持仓卡片、蓝色控件与玻璃底栏。
- 新增首页金额隐藏、盈利/亏损/待报价筛选、代码/市值/收益排序和持仓详情。
- 首页仅保留一行行情状态，详细状态按需点开，异常提示与股票报价日期继续保留。
- 设置精简为操作入口，计算、API、备份、升级和收益说明集中到可离线查看的帮助文档；API 表单支持直达配置帮助。
- 不变更账本存储、备份版本和收益算法。
- 59 项单元测试及 11 项浏览器流程通过；仍需 iPhone 真机覆盖安装确认。


## 1.21 / 1.21.0 / build 4

- 液态玻璃风格浮动导航、半透明控件、圆角弹窗，增加对比度及减少透明度适配。
- 双击缩放防护，显式关闭 iOS WebView 缩放，输入字号不低于 16px。
- 设置新增 Finnhub、自定义 HTTPS API、连接测试、密钥及前台刷新频率。
- 盘中价格独立用于估值，显示报价时间，失败保留上次报价；不进入收盘收益历史或备份。
- API 设置单独保存在本机，密钥不进入备份；核心账本、存储键与应用身份不变。
- 59 项单元测试、8 项浏览器流程测试通过；真机手势与覆盖安装仍待用户验证。


## 1.1.0 / build 2

- 早上打开或返回 App 时更新收盘价，持仓页和收益页增加「更新收益」。
- 纽约交易日、冬夏令时、提前收盘及收盘后 15 分钟等待；仅采纳完成交易日的日线收盘价。
- 逐只显示报价日期和来源；失败时保留价格，显示失败详情和较早日期提示。
- 加载实际公司 Logo，失败回退代码首字母。
- 保持第一版本机存储位置和 JSON 主格式不变；新增来源字段可选。
- 编辑 / 导入 / 手动报价期间保护数据，丢弃过时的并发更新结果。
- 新增旧备份兼容、旧存储升级、交易时段、部分失败与界面竞态测试。

升级前导出第一版备份；使用相同签名身份及应用标识覆盖安装，不要卸载旧版。

## 1.0.0 / build 1

交易记录、移动平均持仓成本、手动报价、已实现与浮动收益、编辑删除、JSON 备份恢复及示例模式。
