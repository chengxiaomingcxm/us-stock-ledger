# 更新记录

## 原生版 1.0.1 · build 6 · 2026-09-19

Portfolio Polish。这一版**没有改收益口径、没有新增交易功能**，重心是首次体验、错误可读性与工程闭环。完整的对外说明见 [releases/v1.0.1.md](releases/v1.0.1.md)。

这一版包含：

- 示例模式：设置里一键载入完整示例账本（买卖、分红、费用、期初余额），示例与真实账本完全隔离。旧版「示例账本」会直接覆盖真实账本，这是本版最重要的修正。
- 错误处理重做：失败时给可读的中文／英文说明，系统原始报错只写进本机诊断日志；修掉「导出失败静默无提示」这条会丢数据的路径。
- 修掉收益页在 320px 宽度下的横向溢出：现金汇总格此前只剩 0.28px 余量，换字体度量就会顶宽整页，已改为可收缩布局。
- 补齐错误提示缺失的英文文案 61 条；修掉 15 条因把数值拼进查找键而永远匹配不到译文的提示。
- 截图与日历渲染链路加非空 / 非空数据守卫，空态截图不能再混进文档。
- 设置改为 显示 / 行情数据 / 数据 / 支持 四组、每个入口一个二级页面；「账本信息」只读，不提供清空或删除账本的操作。
- 首页改用导航栏 `+` 添加交易，移除旧的悬浮按钮，窄屏下不再遮挡内容。
- 英文界面收尾：翻译表补齐到 536 条，英文界面不再出现中文说明。
- 明确 1.0 数据基线：账本格式 `format: 2`；1.0 之前的测试版与旧 Web 版不再是兼容目标，`note` 只保存原文。
- README 中英两版重写，新增 `docs/ARCHITECTURE.md`、`docs/DEVELOPMENT.md`、`docs/CASE_STUDY.md` 与 MIT `LICENSE`。
- 新增第二个 CI 工作流 `checks`（ubuntu-latest），push 与 PR 都跑单测 / 构建 / E2E；macOS 的 `build-ios` 保持不变。
- 全 git 历史 secret 扫描通过，无密钥、Token 或凭据类文件。

安装包显示 1.0.1，构建号递增为 6。应用标识、账本文件、密钥保存位置与数据结构均沿用 1.0.0，覆盖安装即可升级，不会清空数据。

## 原生版 1.0.0 · build 5 · 2026-09-18

将已验收的原生 2.0.0 build 4 定为原生版 1.0 的基线。这次统一版本号、应用内说明和仓库文档，没有新增功能或改变收益计算。

这一版包含：

- SwiftUI 持仓、交易、收益和设置页面，以及快捷记账。
- 移动平均成本、现金账本、每日收益日历和累计曲线。
- Yahoo / Finnhub / 自定义接口，收盘与最新报价模式，前台定时刷新。
- 券商 CSV 预览导入，汇丰投资结单 PDF 的美元交易与分红导入。
- 后台计算收益、复用历史结果、保留曲线图层，以及按实际周数排列的日历。
- 备份恢复、正负号与可选涨跌颜色。
- 中文 / English 界面一键切换，英文模式全界面已本地化。

应用标识、账本文件、密钥保存位置及数据结构沿用原生测试版。安装包显示 1.0.0，构建号继续递增为 5。

用户已确认上一构建代码可用：切换语言、深浅色与记账均无问题。此前的测试与原生检查在 GitHub Actions 全部通过，测试数增至 148 项（新增语言词典重复键回归测试）。本轮补完英文界面遗漏文案，构建结果见 GitHub Actions。模拟器截图不作为真机帧率测量。

仓库新增质量门禁 `scripts/verify.ps1` 与 pre-commit 钩子，提交前自动运行测试与构建。

旧 Web 版和原生测试阶段的记录保存在 [历史更新记录](docs/archive/CHANGELOG.md)，旧标签及 Release 不改写。

本轮验证：源码 `2d8fed3` 的 [GitHub Actions 构建](https://github.com/chengxiaomingcxm/us-stock-ledger/actions/runs/35318039958) 全部通过，包括原有测试、原生账本测试、模拟器截图生成和 iPhoneOS 编译。设备包已核验版本 1.0.0、build 5、ARM64 与 ZIP 完整性。SHA-256：`6dc944aad6874073f6e1abede7a3642dead23cfe92b1a9ba8a541fcbd53f1e60`。

原生版基线标签为 `native-v1.0.0`，与旧 Web 版标签区分。IPA 保存在上述构建附件中，未改写旧 Release。
