import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.personal.stockledger', appName: '持仓账本', webDir: 'dist', ios: { contentInset: 'never', backgroundColor: '#f4f6f8' } };
export default config;
